'use strict';

var RD = (function (rd) {

    rd.dataContext = {};

    rd.dataContext.getNavigation = function() {
        
    };

    rd.dataContext.getContent = function(fileName) {
     
    };

    // Invoke navigation immediatly
    rd.dataContext.getNavigation();

    return rd;

})(RD || {});