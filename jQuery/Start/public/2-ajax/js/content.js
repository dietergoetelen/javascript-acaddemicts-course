'use strict';

var RD = (function (rd) {
    var $content = $('#content');

    function handleContent(_, data) {
       
    }

    rd.content = {};
    rd.content.init = function() {

    };

    rd.content.init();

    return rd;
})(RD || {});